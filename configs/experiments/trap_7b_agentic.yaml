# 7B + curated skills via AGENT-ACTIVATED delivery: only the L0 names+descriptions sit
# in the prompt; skill bodies are sandbox files the agent reads on demand. Same skill
# content as trap_7b_l1 (resolution L1) — only the delivery differs, isolating "does
# faithful delivery close the engagement gap?".
extends: trap_7b.yaml
skills:
  mode: curated
  delivery: agentic
  resolution: L1
  router: forced
  library: statistics
