<script lang="ts">
  // Scaffold shell — the run form, live step stream, and report view land in a later
  // commit. This minimal app keeps the build + type-check green from the first commit.
</script>

<main>
  <header>
    <h1>statskills</h1>
    <p class="tagline">
      Does <em>how</em> a statistics skill is delivered change the answer? Upload a dataset,
      pick a delivery mode, watch the agent work, read the traceable report.
    </p>
  </header>
</main>

<style>
  main {
    max-width: 56rem;
    margin: 2rem auto;
    padding: 0 1rem;
  }
  h1 {
    margin-bottom: 0.25rem;
  }
  .tagline {
    color: var(--muted);
    max-width: 42rem;
  }
</style>
