"""Per-benchmark adapters that normalise external datasets into our ``Task`` (§4).

External formats are translated here and scored by our own verifier — we never fork a
benchmark's evaluation into the harness.
"""
